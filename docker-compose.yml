services:
  shiny:
    image: juraskovakaterina/sequiaviz:latest
    container_name: sequiaviz-shiny
    ports:
      - "8080:8080"
    volumes:
      - ./input_files:/input_files
    depends_on:
      - igv-static
    environment:
      - IGV_HOST=localhost
      - IGV_PORT=8081



  igv-static:
    image: juraskovakaterina/sequiaviz-igv:latest
    container_name: igv-static
    ports:
      - "8081:8081"
    volumes:
      - ./input_files:/srv/igv-static:ro
